<% if (title_helper = page&.title_helper) %>
  <%= send(title_helper, item, page) %>
<% else %>
  <% unless @modal %><h1 class="main-title"><%= page.t(:'form_title.edit') %></h1><% end %>
<% end %>
<%= render partial: 'form', locals: { item: item, page: page } %>
